[project]
name = "yeongi-storage"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "cmp0xff", email = "cmp0xff@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "yeongi-core",
]

[tool.uv.sources]
yeongi-core = { workspace = true }

[build-system]
requires = ["uv_build>=0.10.8,<0.11.0"]
build-backend = "uv_build"
